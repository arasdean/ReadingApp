extends layout

block content
  h1= book.title
  
  h5 Written by #{book.author}
  p=book.body
  hr
  a.btn.btn-default(href='/book/edit/'+book._id) Edit
  a.btn.btn-danger.delete-book(href='#', data-id=book._id) Delete
